version: 0.0
os: linux

files:
  - source: output/portal.html
    destination: /var/www/html/index.html

hooks:
  AfterInstall:
    - location: scripts/restart.sh
      timeout: 300
      runas: root
